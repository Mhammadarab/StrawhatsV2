name: CI - Staging Branch

on:
  push:
    branches:
      - 166-as-a-developer-i-want-to-review-and-implement-a-better-log-format-so-that-the-logs-are-easier-to-analyze-and-maintain
  workflow_dispatch:

env:
  DOTNET_INSTALL_DIR: "./.dotnet"

jobs:
  build:
    uses: ./.github/workflows/ci-cd-build.yml
    with:
      DISCORD_WEBHOOK_URL: ${{ vars.DISCORD_WEBHOOK_URL }}

  test:
    needs: build
    uses: ./.github/workflows/ci-cd-testing.yml
    with:
      DEPLOY_WEBHOOK_URL: ${{ vars.DISCORD_WEBHOOK_URL }}